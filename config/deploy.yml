service: pierre

image: charnould/pierre

servers:
  - 49.13.68.101

registry:
  server: ghcr.io
  username: charnould
  password:
    - REGISTRY_TOKEN

volumes:
  - "telemetry:/app/telemetry/"

env:
  secret:
    - OPENAI_API_KEY
    - REGISTRY_TOKEN
    - AUTH_SECRET
    - PASSWORDS
    - TELEMETRY

healthcheck:
  path: /
