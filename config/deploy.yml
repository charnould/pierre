service: pierre

image: charnould/pierre

servers:
  - 49.13.68.101

registry:
  server: ghcr.io
  username: charnould
  password:
    - REGISTRY_TOKEN

volumes:
  - "telemetry:/app/telemetry/"

env:
  secret:
    - MISTRAL_API_KEY
    - OPENAI_API_KEY
    - TELEMETRY_KEY
    - REGISTRY_TOKEN

healthcheck:
  path: /